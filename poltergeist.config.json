{
  "version": "1.0",
  "projectType": "mixed",
  "targets": [
    {
      "name": "openclaw-cockpit",
      "type": "executable",
      "buildCommand": "mkdir -p ./dist/dev && go build -o ./dist/dev/openclaw-cockpit ./cmd/openclaw-cockpit",
      "outputPath": "./dist/dev/openclaw-cockpit",
      "watchPaths": [
        "cmd/**/*.go",
        "internal/**/*.go",
        "go.mod",
        "go.sum"
      ],
      "environment": {
        "CGO_ENABLED": "0"
      }
    }
  ]
}
